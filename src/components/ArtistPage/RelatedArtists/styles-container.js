import styled from 'styled-components'

import WithContainer from 'components/generic/Container'

const StyledContainerRelatedArtists = styled.div`
  padding: 40px 0;
`

export default WithContainer(StyledContainerRelatedArtists)